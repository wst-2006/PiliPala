# BiliLite 插件系统说明

> 统一插件系统：一个框架承载主题插件 / 功能插件 / 资源包，插件可自由拓展 UI、主题、播放器、数据访问与网络能力。现已实现 6 个阶段全部能力。

## 一、插件包格式

插件 = 一个 zip 包，安装后解压到 `filesDir/plugins/<pluginId>/`：

```
plugin.zip
├── plugin.json      ← 统一清单（必需）
├── main.lua         ← 功能代码（type=feature 或需要代码逻辑时）
├── theme.json       ← 主题声明（可选）
└── assets/          ← 资源（背景图/字体/图标，可选）
```

`plugin.json`：

```json
{
  "id": "com.bililite.theme.ink",
  "name": "水墨主题",
  "version": "1.0.0",
  "apiVersion": 1,
  "type": "theme",
  "author": "",
  "description": "",
  "permissions": ["ui", "player", "data", "network", "system.file"],
  "entry": "main.lua",
  "theme": {}
}
```

- `type`: `theme`（主题）| `feature`（功能）| `resource`（资源）
- `permissions`: 声明要用的能力，只有声明了的 API 才会注入（`ui`/`player`/`data`/`network`/`system`/`system.file`）

## 二、PluginAPI 清单

### ui.*
- `ui.setTheme(themeTable)` — 运行时应用主题（见下方主题字段）
- `ui.getTheme()` — 当前主题语义色 table
- `ui.toast(text)`
- `ui.openUrl(url)`
- `ui.log(text)`

### player.*（需 `player` 权限）
- `player.play()` / `pause()` / `toggle()`
- `player.seekTo(seconds)` / `getPosition()` / `getDuration()`
- `player.setSpeed(speed)` / `getSpeed()`
- `player.setMuted(bool)` / `isMuted()`
- `player.getCurrentVideo()` → table（bvid/cid/title/duration）
- `player.on(event, callback)` — event: play/pause/progress/complete/videoChanged

### data.*（需 `data` 权限）
- `data.getFavorites()` / `addFavorite(bvid)` / `removeFavorite(bvid)`
- `data.getHistory()` / `getBookmarks()` / `getUpList()`
- 返回纯数据 table。

### network.*（需 `network` 权限，带登录态）
- `network.request(path, paramsTable)`
- `network.get(url)`（仅 https）

### events.*
- `events.on(eventName, callback)` / `events.off(eventName)`
- 全局事件：`play`/`pause`/`progress`/`complete`/`videoChanged`/`playerStateChanged`/...

### system.*
- `system.getPluginDir()` / `readFile(name)` / `writeFile(name, content)`（限插件目录）
- `system.getPrefs(key)` / `setPrefs(key, value)`（插件私有 KV）
- `system.log(msg)`

## 三、主题插件

两种方式：

1. **声明式**：`plugin.json` 的 `theme` 字段直接声明颜色/圆角/背景图，加载即生效。
2. **代码式**：`main.lua` 里调 `ui.setTheme({...})`。

主题字段（对应 `C.*` 语义 token）：

```lua
ui.setTheme({
  bg  = "#F5F1E8",     -- 页面背景
  card= "#FFFFFF",     -- 卡片
  t1  = "#3A3A3A",     -- 主文字
  t2  = "#8E8E93",     -- 次文字
  t3  = "#5A5A5A",     -- 正文
  line= "#E0DBD0",     -- 分割线
  block="#4A4A4A",     -- 主按钮/选中态
  soft="#EFEAE0",      -- 输入框底
  onBlock="#FFFFFF",   -- 块上文字
  inputBg="#EFEAE0",   -- 输入框底
  primary="#4A4A4A",   -- 强调
  radius=12,           -- 圆角(dp)
  backgroundImage="bg.jpg"  -- assets 里的背景图文件名
})
```

主题切换**即时生效**，卸载插件恢复默认黑白极简主题。

## 四、沙箱与安全

- 每个插件独立 Lua Globals，不含 `luajava` 反射桥，不含 `io`/`os.execute` 等危险能力。
- 插件崩溃/语法错误 → 仅该插件失效，主程序与其他插件正常。
- 文件操作限插件自身目录，网络仅允许 https。

## 五、打包示例插件

```bash
./pack_plugins.sh
# 产物: plugins_examples/dist/theme_ink.zip 与 feature_autoskip.zip
```

安装：把 zip 放到设备 `filesDir/plugins/` 下（或通过后续的导入入口），在「我的 → 插件 → 重新扫描」加载。

## 六、示例插件

| 插件 | 类型 | 说明 |
|------|------|------|
| `theme_ink` | theme | 水墨主题（灰白配色，声明式） |
| `feature_autoskip` | feature | 自动跳过片头（监听 play/videoChanged + system prefs 存跳过秒数） |