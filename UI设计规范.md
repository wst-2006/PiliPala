# BiliLite UI 设计规范

> 记录 BiliLite 应用的视觉设计语言，供后续项目复用。
> 设计基调：**极简白、圆角苹果风、去彩色干扰**——专为「学习专注型」场景设计，拒绝娱乐化的五颜六色。

---

## 1. 设计理念

- **纯白主题**：整体白色底色，无彩色装饰，视觉安静克制，适合学习/阅读场景。
- **圆角苹果风**：所有卡片、按钮、输入框、图片统一圆角，线条柔和。
- **单一文字主色 + 灰色次级文字**：用「黑 + 灰」两级文字建立信息层级，不用彩色标题。
- **功能色极克制**：仅「收藏星标（金）」「删除/退出（红）」两处有彩色，作为强调而非装饰。

---

## 2. 色彩系统（Color Palette）

| 名称 | 色值 | 用途 |
|------|------|------|
| 背景色（页面底） | `#FAFAFA` | 全局页面背景 |
| 卡片色 | `#FFFFFF` | 卡片 / 面板 / 导航栏背景 |
| 分割线 / 描边 | `#E5E5EA` | 分隔线、未选中描边、图片占位底 |
| 主文字色 | `#1C1C1E` | 标题、正文、选中态文字 |
| 次级文字色 | `#8E8E93` | 说明、时间、粉丝数、占位提示 |
| 正文辅助色 | `#3A3A3C` | 简介、免责声明正文（比主文字略浅） |
| 输入框底 | `#F2F2F7` | 搜索框填充底色 |
| 强调-收藏 | `#FFB300` | 收藏星标（★）选中态 |
| 强调-危险 | `#FF3B30` | 删除 UP、退出登录 |
| 纯黑 | `#000000` | 播放器底色、沉浸式覆盖层 |

> Kotlin 定义（Main.kt）：
> ```kotlin
> val BILIBLACK = Color(0xFFFAFAFA)  // 背景
> val BILICARD  = Color(0xFFFFFFFF)  // 卡片
> val BILILINE  = Color(0xFFE5E5EA)   // 分割线
> ```

---

## 3. 字体（Typography）

- 字体：系统默认无衬线字体（Roboto / PingFang SC），**标题加粗、正文常规**。

| 字号 | 用途 |
|------|------|
| 24sp | App 名称 / 登录页标题 |
| 20sp | 收藏星标、底部导航图标 |
| 18sp | 「我的」用户名 |
| 16sp | 大按钮文字（添加UP主/删除UP主）、QQ群号 |
| 15sp | 视频信息标题、按钮文字 |
| 14sp | 视频卡片标题、UP主名称 |
| 13sp | 正文、分P条目、说明文字 |
| 12sp | 次级信息（粉丝数、时间、播放量、历史时间点） |
| 11sp | 辅助提示、卡片副信息 |

- **字重使用原则**：
  - 标题/用户名/UP主名/视频标题 → `Bold`（粗体）
  - UP主名（列表内）→ `SemiBold`（半粗）
  - 其余 → 默认常规

---

## 4. 圆角（Corner Radius）

| 圆角 | 用途 |
|------|------|
| 22dp | 搜索输入框、搜索按钮（胶囊形） |
| 16dp | 筛选标签（综合/播放量/按时间）、UP筛选标签 |
| 10dp | 播放信息面板、播放器容器 |
| 8dp | 视频封面缩略图、大卡封面 |
| 6dp | 列表小卡封面缩略图 |

- **圆形**：头像统一正圆（`CircleShape`）。

---

## 5. 间距与尺寸（Spacing）

- 页面整体 padding：**16dp**（卡片列表、页面边距）。
- 卡片内边距：**10~12dp**。
- 元素垂直间距常用：**4 / 8 / 12 / 16 / 24 dp**（4dp 为最小步进单位，遵循 4dp 栅格）。
- 元素水平间距常用：**8 / 12 / 16 dp**。

### 关键尺寸

| 组件 | 尺寸 |
|------|------|
| 底部导航栏 | 高度约 56dp |
| 平板侧导航栏（NavigationRail） | 宽 80dp |
| 搜索框 | 高 46dp，圆角 22dp |
| 用户头像（我的页） | 64dp 正圆 |
| UP主头像 | 44dp（搜索结果）/ 40dp（列表）/ 36dp（播放信息面板）正圆 |
| 视频封面 | 横版大卡 140×80dp；列表小卡 120×68dp；竖版卡 16:9 铺满 |
| 播放器（手机半屏） | 占屏高 30%；平板左侧占 2/3 宽 |
| 二维码 | 220dp |

---

## 6. 组件规范

### 6.1 导航栏

- **手机**：底部 `NavigationBar`，2 个 tab（首页 / 我的）。
  - 选中态：文字黑 `#1C1C1E`、指示器底色 `#E5E5EA`。
  - 未选中：文字灰 `#8E8E93`。
- **平板**（宽 ≥600dp）：左侧 `NavigationRail`（竖排，宽 80dp），图标 + 文字。

### 6.2 搜索框

- 圆角胶囊形（22dp），浅灰底 `#F2F2F7`，无下划线。
- 占位文字灰 `#8E8E93`、13sp。
- 与「搜索」按钮同款圆角，视觉统一。

### 6.3 视频卡片

- **列表小卡（手机）**：横版——左封面 + 右侧标题（2 行）+ 副信息（UP主·时长·播放量）+ 收藏星标。
- **竖版卡（平板网格）**：上封面 + 下标题/UP主/播放量。
- 收藏星标：`☆`（灰）未收藏 ↔ `★`（金 `#FFB300`）已收藏。

### 6.4 按钮

- **主按钮**（黑底白字）：`#1C1C1E` 底，白色文字，圆角 22dp，高约 46~52dp。
- **次按钮**（描边）：白底，灰色文字 `#1C1C1E`，`OutlinedButton`。
- **危险按钮**（红色）：删除/退出登录用 `#FF3B30`。

---

## 7. 信息层级原则

1. **一级信息（黑 `#1C1C1E`）**：标题、UP主名、正文——用户最需要看的内容。
2. **二级信息（灰 `#8E8E93`）**：时间、粉丝数、播放量、提示语——辅助上下文。
3. **功能强调（金 `#FFB300` / 红 `#FF3B30`）**：仅收藏、删除两类操作，点到为止。

---

## 8. 播放界面（响应式）

- **手机竖屏**：播放器在上（占屏高 30%），标题/简介/分P 列表在下。
- **平板横屏（宽 ≥600dp）**：左 2/3 播放器 + 右 1/3 信息面板。
  - 信息面板依序：UP主头像+名称 → 发布日期 → 标题 → 播放量 → 简介 → 分P列表。
  - 面板白底 + 圆角 10dp。
- **全屏**：沉浸式（隐藏状态栏/导航栏），播放器铺满；返回手势先退出全屏回到半屏。

---

## 9. 图标策略

- 当前用 emoji 占位（🏠 首页 / 👤 我的 / ★ 收藏）。
- 正式项目建议替换为 Material Symbols（`首页 home`、`我的 person`、`收藏 star`），保持描边风格，与极简白一致。

---

## 10. 复用清单（拷贝即用）

### 颜色常量

```kotlin
val BILIBLACK = Color(0xFFFAFAFA)  // 页面背景
val BILICARD  = Color(0xFFFFFFFF)  // 卡片
val BILILINE  = Color(0xFFE5E5EA)   // 分割线
val TEXT_MAIN  = Color(0xFF1C1C1E)  // 主文字
val TEXT_SUB   = Color(0xFF8E8E93)  // 次级文字
val TEXT_BODY  = Color(0xFF3A3A3C)  // 正文辅助
val INPUT_BG   = Color(0xFFF2F2F7)  // 输入框底色
val ACCENT_FAV = Color(0xFFFFB300)  // 收藏
val ACCENT_DANGER = Color(0xFFFF3B30) // 危险
```

### 常用形状

```kotlin
val RoundedSearch = RoundedCornerShape(22.dp)   // 搜索框/按钮
val RoundedTag    = RoundedCornerShape(16.dp)   // 筛选标签
val RoundedPanel  = RoundedCornerShape(10.dp)   // 面板
val RoundedCover  = RoundedCornerShape(8.dp)    // 封面
val RoundedThumb  = RoundedCornerShape(6.dp)    // 小缩略图
```

### 播放量/粉丝数格式化

```
>= 1亿 → "X.X亿"     >= 1万 → "X.X万"     否则 → 原数
```

---

*最后更新：与 BiliLite 当前 Android 版本一致（白色主题 + 圆角苹果风 + 响应式平板适配）。*
