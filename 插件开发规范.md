# BiliLite 插件开发规范（全量 API）

> 统一插件系统：一个框架承载主题插件 / 功能插件 / 资源包，插件可自由拓展 UI、主题、播放器、数据访问、网络、系统能力与跨插件通信。

## 一、插件包格式

```
plugin.zip
├── plugin.json      ← 统一清单（必需）
├── main.lua         ← 功能代码（type=feature 或需代码逻辑时）
└── assets/          ← 资源（背景图/字体/图标，可选）
```

`plugin.json`：

```json
{
  "id": "com.bililite.feature.xxx",
  "name": "插件名",
  "version": "1.0.0",
  "apiVersion": 1,
  "type": "theme",            // theme | feature | resource
  "author": "",
  "description": "",
  "permissions": ["ui","player","data","network","system","system.file"],
  "entry": "main.lua",
  "theme": { },
  "disable": ["cloudFav"]
}
```

- `permissions`：声明要用的能力，只有声明了的 API 才会注入。
- `disable`：禁用主程序某功能，当前支持 `cloudFav`（禁用 B站收藏入口）。

## 二、全量 API 清单

### ui.*（需 `ui` 权限）
| API | 说明 |
|---|---|
| `ui.toast(text)` | 弹 Toast |
| `ui.dialog(title, message, buttons)` | 弹确认框，`buttons` 为 `{label=callback}` |
| `ui.showInput(title, hint, callback)` | 文本输入框，回调 `callback(text)` |
| `ui.showList(title, itemsTable, callback)` | 列表选择，回调 `callback(selectedText)` |
| `ui.openUrl(url)` | 打开网页 |
| `ui.setTheme(table)` / `ui.getTheme()` | 运行时改主题 / 读主题 |
| `ui.registerMenu(id, label, handler)` | 在「我的」页注册自定义入口 |
| `ui.unregisterMenu(id)` | 移除入口 |
| `ui.log(text)` | 写日志 |

### player.*（需 `player` 权限）
| API | 说明 |
|---|---|
| `play()` / `pause()` / `toggle()` | 播放控制 |
| `seekTo(sec)` / `getPosition()` / `getDuration()` | 进度 |
| `setSpeed(s)` / `getSpeed()` | 倍速 |
| `setMuted(b)` / `isMuted()` | 静音 |
| `setVolume(0~1)` / `getVolume()` | 音量 |
| `getState()` | playing/paused/buffering/ended/idle |
| `getBufferedPercent()` | 缓冲百分比 |
| `getCurrentVideo()` | {bvid,cid,title,duration} |
| `setLoop(b)` | 单集循环 |
| `next()` / `prev()` | 下一集（prev 暂受限） |
| `enableBackground(b)` | 后台播放开关 |
| `on(event, cb)` | play/pause/progress/complete/videoChanged |

### data.*（需 `data` 权限）
| API | 说明 |
|---|---|
| `getFavorites()` / `addFavorite(bvid)` / `removeFavorite(bvid)` | 收藏 |
| `getHistory()` / `clearHistory()` | 历史 |
| `getBookmarks()` / `addBookmark(bvid,title,sec)` / `removeBookmark(bvid)` | 书签 |
| `getUpList()` / `addUp(mid,name)` / `removeUp(mid)` | UP 主 |
| `getUpVideos(mid)` | 某 UP 全部视频 |
| `setWatched(videoId, watched)` | 已看标记 |

### network.*（需 `network` 权限）
| API | 说明 |
|---|---|
| `get(url)` | GET 请求（仅 https + bilibili.com 白名单） |
| `request(path, paramsTable)` | GET + 参数（带登录态） |
| `post(url, paramsTable)` | POST x-www-form-urlencoded（带登录态） |

### events.*
| API | 说明 |
|---|---|
| `on(eventName, cb)` / `off(eventName)` | 事件订阅 |
| 事件 | play/pause/progress/complete/videoChanged/playerStateChanged |

### system.*
| API | 说明 |
|---|---|
| `log(msg)` | 日志 |
| `getPrefs(key)` / `setPrefs(key, value)` | 插件私有 KV |
| `getPluginDir()` | 插件目录 |
| `readFile(name)` / `writeFile(name, content)` | 文件读写（限插件目录，需 `system.file`） |
| `listFiles()` / `deleteFile(name)` | 文件列表/删除（需 `system.file`） |
| `setBrightness(0~1)` | 窗口亮度 |
| `setVolume(0~1)` | 媒体音量 |
| `getClipboard()` / `setClipboard(text)` | 剪贴板 |
| `vibrate(ms)` | 震动 |
| `share(text)` | 系统分享 |

### storage.*（跨插件共享存储）
- `storage.set(key, value)` / `storage.get(key)`

### plugin.*（跨插件通信）
- `plugin.export(name, fn)` — 导出函数
- `plugin.call(pluginId, name)` — 调用其它插件导出的函数

### timer.*（定时任务）
- `timer.schedule(delayMs, repeatMs, callback)` — `repeatMs<=0` 执行一次，否则周期执行

## 三、主题插件

声明式（plugin.json 的 theme 字段）或代码式（`ui.setTheme`）。字段：`bg/card/t1/t2/t3/line/block/soft/onBlock/inputBg/primary/radius/backgroundImage/backgroundImageTablet`。

`backgroundImage`（手机竖图）+ `backgroundImageTablet`（平板横图），颜色 `#RRGGBB` 或 `#AARRGGBB`。

## 四、沙箱与安全

- 独立 Lua 环境，不含 `luajava`/`io`/`os.execute`。
- 网络仅 https + bilibili.com 域名白名单。
- 文件操作限插件目录，路径有防穿越校验。
- 插件崩溃不影响主进程。

## 五、何时需要更新 APK

- 新增/修改 API 底层能力、修 App 自身 bug → 需要。
- 仅用已有 API 的逻辑性插件 → 不需要。

## 六、示例插件

| 插件 | 类型 | 说明 |
|---|---|---|
| `theme_ink` | theme | 水墨主题 |
| `theme_miku` | theme | 初音主题（蓝色双背景图） |
| `theme_wild` | theme | 原野主题（绿色护眼双背景图） |
| `feature_autoskip` | feature | 自动跳片头 |
| `feature_disable_cloudfav` | feature | 禁用 B站收藏 |
| `feature_subtitle_size` | feature | 字幕大小调节 |
